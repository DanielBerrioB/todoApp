const taskStyles = (theme) => ({
  gridList: {
    marginTop: "20px",
  },
  titleTask: {
    textAlign: "center",
  },
  itemStyle: {
    backgroundColor: theme.palette.primary.main,
    borderRadius: "5px",
  },
  titleBar: {
    background: "none",
  },
});

export default taskStyles;
